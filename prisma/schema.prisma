generator client {
  provider = "prisma-client-js"

}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Article {
  id          Int          @id @default(autoincrement())
  imageUrl       String
  title       String       @unique
  description String?
  body        String
  category  Category @default(TECH_NEWS)
  published   Boolean      @default(false)
  createAte   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
}

enum Category {
  TUTORIAL
  TECH_NEWS
  FINANCE
}
